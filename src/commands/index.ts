import announce from "./announce";
import initRolePicker from "./init-role-picker";

const getCommands = () => {
    return [
        announce,
        initRolePicker
    ]
}

export default getCommands;
